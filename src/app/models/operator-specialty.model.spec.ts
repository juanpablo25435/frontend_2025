import { OperatorSpecialty } from './operator-specialty.model';

describe('OperatorSpecialty', () => {
  it('should create an instance', () => {
    expect(new OperatorSpecialty()).toBeTruthy();
  });
});
